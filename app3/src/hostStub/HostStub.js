import React from "react";

// This is where we can stub out a host app and render App 3 to test it out without
// needing to run real host apps.
const HostStub = () => (
  <div>
    Host Stub for App 3. This is where we can stub out a host app and render App
    3 to test it out without needing to run real host apps.
  </div>
);

export default HostStub;
